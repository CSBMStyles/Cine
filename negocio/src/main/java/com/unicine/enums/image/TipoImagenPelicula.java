package com.unicine.enums.image;

/**
 * Representa el tipo de imagen asociada a una película.
 * <p>
 * El tipo determina el perfil de redimensionamiento aplicado por el procesador
 * de imágenes al subir la imagen a ImageKit.
 */
public enum TipoImagenPelicula {
    POSTER,
    BANNER
}
