meta {
  name: obtener - GET /api/colecciones/{cedula}/{pelicula}
  type: http
  seq: 3
}

vars:pre-request {
  baseUrl: http://localhost:8080
}

get {
  url: {{baseUrl}}/api/colecciones/1009000011/1
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

docs {
  Clave compuesta (cedula, pelicula). 200 si existe, 404 si no.
}
