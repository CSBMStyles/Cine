meta {
  name: listar - GET /api/colecciones
  type: http
  seq: 2
}

vars:pre-request {
  baseUrl: http://localhost:8080
}

get {
  url: {{baseUrl}}/api/colecciones?cliente=1009000011
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

docs {
  Requiere auth. Filtros: ?cliente=, ?pelicula=, ?page=&size=&sort=
}
