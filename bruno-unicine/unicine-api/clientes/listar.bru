meta {
  name: listar - GET /api/clientes
  type: http
  seq: 2
}

vars:pre-request {
  baseUrl: http://localhost:8080
}

get {
  url: {{baseUrl}}/api/clientes
  auth: inherit
}

docs {
  Solo rol ADMINISTRADOR. 401 sin auth, 403 si CLIENTE, 200 con lista sin password.
}
