meta {
  name: listar - GET /api/pelicula-disposiciones
  type: http
  seq: 2
}

vars:pre-request {
  baseUrl: http://localhost:8080
}

get {
  url: {{baseUrl}}/api/pelicula-disposiciones?page=0&size=5
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

docs {
  Requiere auth. Soporta paginacion ?page=&size=&sort=
}
