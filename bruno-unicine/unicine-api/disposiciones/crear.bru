meta {
  name: crear - POST /api/pelicula-disposiciones
  type: http
  seq: 1
}

vars:pre-request {
  baseUrl: http://localhost:8080
}

post {
  url: {{baseUrl}}/api/pelicula-disposiciones
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "peliculaCodigo": 1,
    "ciudadCodigo": 1,
    "estadoPelicula": "PENDIENTE"
  }
}
